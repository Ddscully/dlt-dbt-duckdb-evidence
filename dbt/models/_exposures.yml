version: 2

# Who reads this warehouse, declared to dbt.
#
# `dbt ls --select +exposure:evidence_retail` answers "what breaks if I change
# this model" for one page; `+exposure:*` for every consumer.
#
# Not a duplicate of `TABLE_TO_DBT_MODEL` in `publish/build_report.py`: that map
# is per *site* (the Dagster graph's edge, Polars tables included), these are per
# *page* and can only name nodes dbt builds. `tests/test_exposures.py` resolves
# page → source query → warehouse table and fails if the two disagree.
#
# dbt cannot see the Polars outputs in `analytics` (`co2_intensity`,
# `retail_rfm`, the four `pipeline_*` tables), so the Findings, Countries and
# Retail exposures each name one fewer dependency than their page reads; the test
# asserts that remainder is exactly `TABLE_TO_ASSET_KEY`. Two pages have no
# exposure: `pipeline.md` reads only Polars tables and `index.md` reads nothing.
#
# `maturity:` is not set: every page deploys from one workflow on one schedule.
exposures:
  # Named `evidence_<page file slug>`, which the test maps mechanically.
  #
  # The page is `countries.md`, not `explore.md`: **`explore` is a reserved
  # Evidence route** (the template's SQL console), so a page of that name is never
  # copied in and the build dies on `/api/explore/evidencemeta.json` 500.
  # `settings` and `api` are reserved too.
  - name: evidence_countries
    label: Dashboard — Country Explorer
    type: dashboard
    url: https://ddscully.github.io/dlt-dbt-duckdb-evidence/countries/
    description: >
      The self-serve country explorer: pick a year and every chart re-queries.
      Headline emissions, the decarbonisation ranking and the European
      electricity price panel. Also reads `analytics.co2_intensity`, which dbt
      does not build.
    depends_on:
      - ref('fct_emissions_energy')
      - ref('fct_eu_electricity_prices_semiannual')
    owner:
      name: Daniel Scully
      email: danieldscully@gmail.com

  - name: evidence_findings
    label: Dashboard — Findings
    type: dashboard
    url: https://ddscully.github.io/dlt-dbt-duckdb-evidence/findings/
    description: >
      The eight findings, each ending in a decision. The most load-bearing page
      on the site: every number in it is quoted in `README.md` and
      `docs/FOR_REVIEWERS.md`, so a change to `fct_emissions_energy` that moves
      one of them has to move the prose too. Also reads
      `analytics.co2_intensity`.
    depends_on:
      - ref('fct_emissions_energy')
    owner:
      name: Daniel Scully
      email: danieldscully@gmail.com

  - name: evidence_coverage
    label: Dashboard — Coverage
    type: dashboard
    url: https://ddscully.github.io/dlt-dbt-duckdb-evidence/coverage/
    description: >
      What the warehouse does and does not cover, per source and per column.
      Depends on the spine as well as the fact, because coverage gaps are only
      visible as rows against the complete country-year cross join.
    depends_on:
      - ref('dim_country_year')
      - ref('fct_emissions_energy')
    owner:
      name: Daniel Scully
      email: danieldscully@gmail.com

  - name: evidence_restatements
    label: Dashboard — Restatements
    type: dashboard
    url: https://ddscully.github.io/dlt-dbt-duckdb-evidence/restatements/
    description: >
      Revision history of OWID's CO2 estimates, off the snapshot. The one page
      whose content cannot be rebuilt from the sources — see
      `history.snap_co2_estimates`.
    depends_on:
      - ref('fct_co2_estimate_versions')
    owner:
      name: Daniel Scully
      email: danieldscully@gmail.com

  - name: evidence_scope2
    label: Dashboard — Scope 2 factors
    type: dashboard
    url: https://ddscully.github.io/dlt-dbt-duckdb-evidence/scope2/
    description: >
      Grid emission factors as a reference product, with the vintage problem and
      the three GHG Protocol caveats stated on the page. The worked example it
      renders is the only fabricated data in the warehouse and is labelled as
      such in three places.
    depends_on:
      - ref('dim_grid_emission_factors')
      - ref('fct_example_scope2_emissions')
    owner:
      name: Daniel Scully
      email: danieldscully@gmail.com

  - name: evidence_cbam
    label: Dashboard — CBAM exposure
    type: dashboard
    url: https://ddscully.github.io/dlt-dbt-duckdb-evidence/cbam/
    description: >
      The border cost per sourcing country and good, priced at the
      `eu_ets_price_eur_per_t` var. The page derives its whole EUR 60-120
      sensitivity band from the tonnage columns in one build, so a re-price does
      not need a rebuild — but a change to the mart's *columns* does break it.
    depends_on:
      - ref('fct_cbam_exposure')
    owner:
      name: Daniel Scully
      email: danieldscully@gmail.com

  - name: evidence_currency
    label: Dashboard — Currency
    type: dashboard
    url: https://ddscully.github.io/dlt-dbt-duckdb-evidence/currency/
    description: >
      The FX layer: the calendar gap, the currency panel's retirements, spot
      against average, and the euro-versus-dollar reading of the Eurostat
      electricity price. Reads more marts than any other page.
    depends_on:
      - ref('dim_currency')
      - ref('dim_date')
      - ref('fct_eu_electricity_prices_semiannual')
      - ref('fct_fx_rates_daily')
      - ref('fct_fx_rates_periods')
      - ref('fct_fx_rates_published')
    owner:
      name: Daniel Scully
      email: danieldscully@gmail.com

  # Includes `dim_country_year`, read only for chart labels: a label source still
  # breaks the page when it changes.
  - name: evidence_weather
    label: Dashboard — Weather
    type: dashboard
    url: https://ddscully.github.io/dlt-dbt-duckdb-evidence/weather/
    description: >
      Capital-city degree days as a control variable: whether a year was colder,
      tested against what electricity prices did in the same year and found to
      explain none of it. Also carries the two degree-day conventions, the
      capital-as-proxy distance, and why the current year is filtered out of
      every comparison.
    depends_on:
      - ref('dim_country_year')
      - ref('fct_country_weather_year')
      - ref('fct_eu_electricity_prices_semiannual')
    owner:
      name: Daniel Scully
      email: danieldscully@gmail.com

  - name: evidence_retail
    label: Dashboard — Retail
    type: dashboard
    url: https://ddscully.github.io/dlt-dbt-duckdb-evidence/retail/
    description: >
      The transactional grain: line types, returns matched by inference, the
      cohort triangle and the RFM segments. Also reads `analytics.retail_rfm`,
      which is a Polars output and so cannot appear below.
    depends_on:
      - ref('fct_retail_order_line')
      - ref('fct_retail_returns')
      - ref('fct_retail_customer_cohorts')
      - ref('dim_retail_customer')
    owner:
      name: Daniel Scully
      email: danieldscully@gmail.com

  # The monthly GitHub release: the one consumer outside the project, and why
  # every mart is `access: public`. Exactly the marts — the staging layer ships
  # as Parquet too (`PUBLISHED_SCHEMAS`), but the release notes promise nothing
  # about it.
  - name: published_data_release
    label: Monthly data release (GitHub)
    type: application
    url: https://github.com/ddscully/dlt-dbt-duckdb-evidence/releases
    description: >
      `data-YYYY-MM-DD`: the DuckDB file, a zstd Parquet per modelled table,
      `manifest.json`, `SHA256SUMS` and `ATTRIBUTION.md`, built monthly by
      `release-data.yml` against live sources. Consumers are outside this repo
      and cannot be paged, so a column removed here is a breaking change with no
      migration path — which is what `contract: {enforced: true}` on the marts is
      for, and why the versioned model exists.
    depends_on:
      - ref('dim_country')
      - ref('dim_country_income_history')
      - ref('dim_country_year')
      - ref('dim_currency')
      - ref('dim_date')
      - ref('dim_grid_emission_factors')
      - ref('dim_retail_customer')
      - ref('dim_retail_product')
      - ref('fct_cbam_exposure')
      - ref('fct_co2_estimate_versions')
      - ref('fct_country_weather_year')
      - ref('fct_emissions_energy')
      - ref('fct_eu_electricity_prices_semiannual')
      - ref('fct_example_scope2_emissions')
      - ref('fct_fx_rates_daily')
      - ref('fct_fx_rates_periods')
      - ref('fct_fx_rates_published')
      - ref('fct_retail_customer_cohorts')
      - ref('fct_retail_order_line')
      - ref('fct_retail_returns')
    owner:
      name: Daniel Scully
      email: danieldscully@gmail.com
