-- The currency dimension: 47 rows, so no filtering to do. The page reads it for
-- the lifecycle section — which codes stopped, when, and whether the seed has a
-- reason for it.
select * from marts.dim_currency
